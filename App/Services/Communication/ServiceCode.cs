namespace rp.Accounting.App.Services.Communication
{
    public enum ServiceCode
    {
        Ok = 200,
        NoContent = 204,
        BadRequest = 400,
        NotFound = 404,
        Conflict = 409,
        InternalServerError = 500
    }
}
